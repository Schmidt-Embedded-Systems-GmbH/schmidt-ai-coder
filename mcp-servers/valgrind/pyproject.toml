[project]
name = "Valgrind-MCP-Server"
version = "0.1.0"
description = "Valgrind MCP server for memory analysis"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastmcp>=2.14.1,<3",
    "mcp[cli]>=1.14.0",
]
