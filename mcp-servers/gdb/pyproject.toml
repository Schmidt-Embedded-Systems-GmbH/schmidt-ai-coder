[project]
name = "gdb-mcp-server"
version = "0.2.0"
description = "GDB MCP server for debugging - modular architecture"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastmcp>=2.14.1,<3",
    "mcp[cli]>=1.14.0",
]

[tool.uv]
dev-dependencies = []
