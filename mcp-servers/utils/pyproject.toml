[project]
name = "Utils-MCP-Server"
version = "0.1.0"
description = "Utils MCP server for lightweight static-analysis CLI utilities"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastmcp>=2.14.1,<3",
    "mcp[cli]>=1.14.0",
]
