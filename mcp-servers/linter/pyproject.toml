[project]
name = "linter"
version = "0.1.0"
description = "Linter MCP Server with VS Code API Bridge"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastmcp>=2.14.1,<3",
    "mcp[cli]>=1.14.0",
    "websockets>=15.0.1",
]
